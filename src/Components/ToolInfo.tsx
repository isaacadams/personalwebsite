import {NavPageComponent} from './Shared/NavPageComponent';
import React from 'react';

/* export class ToolInfo extends NavPageComponent {
    
    constructor(props){
        super(props);

        this.state = {
            text: "write some text."
        };
    }
    
    onTextAreaChange(text: string) {
        
        this.setState({
            text
        });
    }
    
    render() {
        return (<div>
            <div className="row">
                <div className="col-6">
                    <textarea 
                        className="form-control" 
                        placeholder={this.state.text} 
                        onChange={(e) => this.onTextAreaChange(e.currentTarget.value)}>
                    </textarea>
                </div>
                <div className="col-6">
                    <div dangerouslySetInnerHTML={{ __html: this.state.text }} />
                </div>
            </div>
        </div>);
    }
} */
